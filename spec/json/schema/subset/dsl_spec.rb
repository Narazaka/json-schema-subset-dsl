RSpec.describe Json::Schema::Subset::Dsl do
  it "has a version number" do
    expect(Json::Schema::Subset::Dsl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
